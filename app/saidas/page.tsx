import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Saídas",
    description: "Veja um resumo das suas finanças.",
  };


const ExitsPage = () => {
    return ( <h1>Exits Page</h1> );
}
 
export default ExitsPage;